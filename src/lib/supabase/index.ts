// Browser-safe client — safe to import anywhere
export { supabase } from "./client";

// Server helpers — only import inside src/server/**
// export { getAdminClient, getServerClient } from "./server";
