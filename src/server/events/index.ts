/**
 * Event / analytics server functions
 *
 * TODO: capture QR scan events, page views, conversion events.
 *
 * Table: events (type, restaurant_id, campaign_id, metadata, created_at)
 * Can be written to Supabase directly or piped through a queue (e.g. Cloudflare Queues).
 */
